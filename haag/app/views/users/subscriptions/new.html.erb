<p>Available classes
<%= form_tag( user_subscriptions_path(@user), :method => :post ) do %>
<%= 
  select_tag( 
    :course_id, 
    options_from_collection_for_select(
      @unsubscribed_courses, 
      'id', 
      'name', 
      @default_course
    ) 
  ) 
%>
<%= submit_tag 'Subscribe', :class => 'btn btn-inverse' %> or <%= link_to 'Cancel', root_path %>
<% end %>
</p>

