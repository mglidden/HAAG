<br/><br/>
<h2>Looking for the calendar? It is in your <%= link_to 'dashboard', root_path %>.</h2>
<br/><br/>

<h1>Listing courses</h1>

<table class='index'>
  <tr>
    <th>Short name</th>
    <th>Long name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @courses.each do |course| %>
  <tr>
    <td><%= course.short_name %></td>
    <td><%= course.long_name %></td>
    <td><%= link_to 'Show', course_assignments_path(course) %></td>
    <td><%= link_to 'Edit', edit_course_path(course) %></td>
    <td><%= link_to 'Destroy', course, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Course', new_course_path %>
